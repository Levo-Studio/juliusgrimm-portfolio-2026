name: Build and Push Docker Image

on:
  push:
    branches:
      - main
    paths-ignore:
      - "k8s/**"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  docker:
    if: "!contains(github.event.head_commit.message, 'Update from image update automation')"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          build-args: |
            DATABASE_URL=postgresql://build:build@localhost:5432/build
          tags: |
            ghcr.io/levo-studio/juliusgrimm-portfolio-2026:latest
            ghcr.io/levo-studio/juliusgrimm-portfolio-2026:${{ github.sha }}
            ghcr.io/levo-studio/juliusgrimm-portfolio-2026:build-${{ github.run_number }}
